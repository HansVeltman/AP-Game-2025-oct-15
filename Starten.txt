pip install websockets

cd backend
python backend.py

cd frontend
python -m http.server 8030

http://localhost:8030



Push to GitHUb:

git status                            # even checken
git add -A                            # ALLES stagën (incl. deletes)
git commit -am "Restruct code"        # beschrijf je wijziging
git push origin main                  # naar GitHub


